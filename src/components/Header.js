import React from "react";
import {StyledHeader, StyledTitle} from "./styles";

const Header = () => {
    return (
        <StyledHeader>
            <StyledTitle>
                <h1>Shopping Cart</h1>
            </StyledTitle>
        </StyledHeader>
    )
}

export default Header;